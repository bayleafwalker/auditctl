{
  "schema_version": "verification-result/v1",
  "context_id": "auditctl.central-observation-ingest",
  "contract_ref": {
    "doc_id": "auditctl-vuoro-served-audit-alignment",
    "revision": "git:0751c6a77cacc5a2dd016705a5d2e9756a3216f2"
  },
  "implementation_sha": "sha256:central-implementation-v1:05bded7e9c781caa3ddf1b938ad8a1a66878f3dbfe13f51c8658f5ef549eb3dc",
  "depth": 2,
  "environment": {
    "backend": "disposable-local-postgresql",
    "isolation": "PostgreSQL READ COMMITTED transactions with per-schema advisory migration locks, per-event advisory locks, and per-origin stream row locks",
    "tool_versions": {
      "postgresql": "18.6",
      "python": "3.12.14",
      "pytest": "9.1.1"
    }
  },
  "execution": {
    "seed": null,
    "kind": "re-verification-of-a-changed-implementation",
    "suite": "tests/ under PostgreSQL realized through `nix shell nixpkgs#postgresql`",
    "passed": 147,
    "failed": 0,
    "counterexamples_found": 0
  },
  "claims": [
    {
      "property": "the digested central implementation differs from the tree observed by the record-class result (cba8850) only in auditctl.dispatch.json's local-tooling paths and pyproject.toml's version literal",
      "result": "established-by-diff",
      "evidence": [
        "`git diff cba8850..HEAD` over the eight CENTRAL_IMPLEMENTATION_PATHS touches only auditctl.dispatch.json, changing `skills.template_root` and the validate_verification_artifacts.py invocation from `/projects/dev/agentops/templates/dispatch/...` to `/projects/dev/agentops/...`; none of central.py, the central_migrations tree, or central_schema.py changed",
        "the only further difference in this working tree is pyproject.toml's `version = \"0.1.7\"` -> `\"0.1.8\"`, made for this release",
        "dispatch.json's changed keys steer local dispatch tooling (skill template location, an artifact-validation script path) and are not read by the central ingest, migration, or schema code exercised by this suite"
      ]
    },
    {
      "property": "the central observation-ingest suite, including the disposable-PostgreSQL integration tests, passes against this tree",
      "result": "observed",
      "evidence": [
        "147 passed, 0 failed, 1 skipped on 2026-09-17 with PostgreSQL 18.6 realized per docs/operations/running-the-central-verification.md (`nix shell nixpkgs#postgresql`)",
        "the sole skip is test_built_wheel_satisfies_release_contract, which only runs once the release workflow has built a dist/ wheel",
        "the run taken before this packet existed was 137 passed, 1 failed, 11 skipped without PostgreSQL binaries on PATH -- once nix supplied PostgreSQL 18.6, the ten previously-skipped central integration tests ran and passed, leaving only this digest assertion failing until this packet was added"
      ]
    }
  ],
  "counterexamples": [],
  "notes": [
    "This is a re-verification for the 0.1.8 release. It does NOT restate the fault enumeration, history count or per-invariant evidence recorded by item-1201 or the record-class result, because this run did not independently instrument them; those results remain the record of what they each observed and are unchanged.",
    "The reason a new packet exists at all: pyproject.toml is inside the digested implementation set, so a version bump changes the digest of an implementation that did not change. The assertion requires that some result for this context was observed against the currently packaged tree, so a packet is added rather than an existing one restamped.",
    "The PostgreSQL instance was created under a temporary directory by the test fixture and stopped and removed afterward; no shared or production database was addressed.",
    "auditctl has no local sprintctl tracking; this release was prepared as a direct operator request rather than a numbered work item."
  ]
}
